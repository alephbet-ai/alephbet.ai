# Sanity connection. projectId and dataset are safe to expose publicly —
# they identify the content API, not credentials. Copy to `.env` to run locally.
PUBLIC_SANITY_PROJECT_ID=z3owbx4h
PUBLIC_SANITY_DATASET=production
